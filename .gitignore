/config/
/.env

# Copia de trabajo de las pruebas de la API: contiene un token real.
# Versionamos solo la plantilla lotes.http.example.
/api/v1/lotes.http

# PDFs de estados de cuenta generados en producción (datos financieros de
# clientes reales): no se versionan, solo la carpeta vía .gitkeep.
/uploads/estados_cuenta/*.pdf
